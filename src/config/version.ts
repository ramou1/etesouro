/**
 * Versão do Sistema
 * 
 * INSTRUÇÕES PARA ATUALIZAR A VERSÃO:
 * 1. A cada novo deploy, atualize o valor de APP_VERSION abaixo
 * 2. Use o formato semântico: MAJOR.MINOR.PATCH (ex: 1.0.0, 1.0.1, 1.1.0, 2.0.0)
 *    - MAJOR: mudanças incompatíveis (ex: refatoração grande)
 *    - MINOR: novas funcionalidades (ex: adicionar feature)
 *    - PATCH: correções de bugs (ex: corrigir erro)
 * 3. Opcionalmente, atualize LAST_UPDATE com a data do deploy
 * 
 * A versão será exibida automaticamente no rodapé de todas as páginas principais.
 */

// Versão atual do sistema
export const APP_VERSION = '1.0.0';

// Data da última atualização (opcional, formato: YYYY-MM-DD)
export const LAST_UPDATE = '2025-12-19';

